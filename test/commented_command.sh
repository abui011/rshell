#tests commands with comments


echo Checking for comments #ls -a
echo Comments work
ls -a #echo this should not display
echo "#this comment should work though"
echo "this one on the right shouldn't work though" #this one right here
exit
